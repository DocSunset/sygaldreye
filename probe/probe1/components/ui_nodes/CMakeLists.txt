add_library(ui_nodes STATIC ui_nodes.cpp)
target_include_directories(ui_nodes PUBLIC . $ENV{EIGEN3_INCLUDE_DIR})
target_link_libraries(ui_nodes PUBLIC tri_mesh render_payloads sygaldry_endpoints)
target_compile_options(ui_nodes PRIVATE -Wall -Wextra -Werror)

if(NOT CMAKE_CROSSCOMPILING)
    find_package(GTest REQUIRED)
    add_executable(ui_nodes_test ui_nodes.test.cpp)
    target_link_libraries(ui_nodes_test PRIVATE ui_nodes GTest::gtest_main)
    target_compile_options(ui_nodes_test PRIVATE -Wall -Wextra)
endif()
