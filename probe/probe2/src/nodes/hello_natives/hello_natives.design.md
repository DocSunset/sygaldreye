# hello_natives — the first linked vocabulary

Owning package: `nodes`. Clauses: floor (osc/lfo/vca — ugens wrappers) and
scaffolding (dac placeholder sink). Allowed dependencies: `crown`, `ugens`.
The tape instantiates these by name; their registration tables fold into
the generated registration TU when SZ-2 lands (rung 4).
