# testnodes — the contract-audit fixtures

Owning package: `nodes`. Clause: **fixture** (ADR-036) — permanent audit
nodes, not scaffolding: a deliberate-violation probe (an RT-allocator, a
fault injector) can never be a well-behaved real native, so the CNF-1
dissolution a prior marker anticipated does not apply. Allowed dependencies:
`authoring`, `abi`. Four bodies whose generated shells demonstrate ch. 13:
declared fault conversion (parsey), undeclared-throw death (boom), and the
prepare-only resource rule (devicey / devicey_bad).
