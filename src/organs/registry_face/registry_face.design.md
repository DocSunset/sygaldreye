# registry_face — the registry organ

Owning package: `organs`. Clause: **machinery** (on the shrink list: may
reduce to a store query over a frozen store-graph). Allowed dependencies:
`crown`, nlohmann_json; the DEFINITION of `registered_natives()` is the
GENERATED per-target registration TU (SZ-2) — natives are present by
linkage, omission is a loud link error, and the palette equals the
manifest by construction (both are projections of the same generated list).
