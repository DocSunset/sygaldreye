# synth_core — the salvaged kernel set

Owning package: `nodes`. Clause: **floor** (per-sample kernels, AUT-1).
Salvaged VERBATIM from `probe/components/synth_core` (appendix A salvage
list) — the probe's design doc still describes it; only the clause marker
line was added. Allowed dependencies: none (cmath only — freeze tier 1).
