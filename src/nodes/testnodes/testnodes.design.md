# testnodes — the contract-audit scaffolding

Owning package: `nodes`. Clause: **scaffolding** (dissolves when real
fallible/resource natives exercise the same shells). Allowed dependencies:
`authoring`, `abi`. Four bodies whose generated shells demonstrate ch. 13:
declared fault conversion (parsey), undeclared-throw death (boom), and the
prepare-only resource rule (devicey / devicey_bad).
