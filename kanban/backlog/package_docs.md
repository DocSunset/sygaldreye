# Package design docs don't exist

Zero `package_name.design.md` files despite CLAUDE.md requiring them,
and CLAUDE.md's 4-package architecture (platform/xr/render/scene, with
"scene: TBD" and "Goal: TODO") no longer describes the app (peer_core,
signal_graph, render_region, audio_region, the editor nodes…). Ratify
a current package map with Travis, write the package design docs, and
fix CLAUDE.md's architecture + goal sections.
reports/audit_conformability_editor_arc.md §7.
