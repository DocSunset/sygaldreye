# Rename project to sygaldreye

Rename the project from `eyeballs` to `sygaldreye` everywhere:

- Repository/directory name
- `CMakeLists.txt` project name
- Android package name / app label
- `CLAUDE.md` references
- `planning/` documents
- Any string literals, log tags, mDNS service names (`_eyeballs._tcp.local.` →
  `_sygaldreye._tcp.local.`)
- README if one exists
