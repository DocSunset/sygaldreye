if(CMAKE_CROSSCOMPILING)
    add_library(vr_editor STATIC vr_editor.cpp)
    target_include_directories(vr_editor PUBLIC .)
    target_include_directories(vr_editor PUBLIC $ENV{EIGEN3_INCLUDE_DIR})
    target_link_libraries(vr_editor PUBLIC vr_panel ray_selector signal_graph text_mesh)
    target_compile_options(vr_editor PRIVATE -Wall -Wextra)
endif()
