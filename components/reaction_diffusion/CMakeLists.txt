add_library(reaction_diffusion STATIC reaction_diffusion.cpp)
target_include_directories(reaction_diffusion PUBLIC . $ENV{EIGEN3_INCLUDE_DIR})
target_link_libraries(reaction_diffusion PUBLIC tri_mesh gl_program sygaldry_endpoints PRIVATE log_component)
target_compile_options(reaction_diffusion PRIVATE -Wall -Wextra)

if(CMAKE_CROSSCOMPILING)
    target_link_libraries(reaction_diffusion PRIVATE GLESv3)
else()
    target_link_libraries(reaction_diffusion PRIVATE GLESv2)
endif()
