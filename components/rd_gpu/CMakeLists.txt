if(CMAKE_CROSSCOMPILING)
    set(_gles GLESv3)
else()
    set(_gles GLESv2)
endif()
add_library(rd_gpu STATIC rd_gpu.cpp)
target_include_directories(rd_gpu PUBLIC .)
target_link_libraries(rd_gpu PUBLIC gpu_texture sygaldry_endpoints log_component ${_gles})
target_compile_options(rd_gpu PRIVATE -Wall -Wextra)
