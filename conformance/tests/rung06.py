"""Rung 6 — stubs. Each entry: criterion id -> callable or None (pending).
Write the test when you reach this rung; the assertion is quoted from the book.
Never weaken a test to pass it — amend the book by ADR instead."""

TESTS = {
    # 11-language-core.md: \"takes whose lineage includes CID_osc-type\" wired from the four
    "LNG-10.1": None,
    # 11-language-core.md: the standing form updates within one demand cycle of a new
    "LNG-10.2": None,
    # 11-language-core.md: fixpoint over a cyclic link structure terminates (visited-set
    "LNG-10.3": None,
    # 11-language-core.md: the palette and the back-link index are expressed as queries
    "LNG-10.4": None,
    # 03-store.md: put(bytes) to hash; get(hash) to identical bytes; get of unknown
    "STO-1.1": None,
    # 03-store.md: hello-cosine's editor session reads #b22 by wiring to the store
    "STO-2.1": None,
    # 03-store.md: compiling #a11 yields an execution-graph dataset whose provenance
    "STO-3.1": None,
    # 03-store.md: recording 2 s of dac output yields a wav dataset whose testimony
    "STO-3.2": None,
    # 03-store.md: edit hello-cosine (freq 220 to 330), commit, undo: ref points at
    "STO-4.1": None,
    # 03-store.md: Quest holds #a11 unprovided; after cache pressure, #a11 is gone
    "STO-5.1": None,
    # 03-store.md: a fresh capture on Quest reports `provided-by: nobody` until the
    "STO-5.2": None,
    # 03-store.md: interrupting a 100 MB take transfer at 50% and retrying moves only
    "STO-6.1": None,
    # 03-store.md: two presets of hello-cosine share topology hash #b22 (observable:
    "STO-7.1": None,
    # 03-store.md: deleting osc0 from the topology surfaces a conflict for the
    "STO-7.2": None,
    # 03-store.md: integration test across three peers confirms convergence and that
    "STO-8.1": None,
    # 03-store.md: the index is itself derived data — deleting and re-deriving it
    "STO-9.1": None,
}
