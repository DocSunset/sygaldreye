"""Rung 3 — stubs. Each entry: criterion id -> callable or None (pending).
Write the test when you reach this rung; the assertion is quoted from the book.
Never weaken a test to pass it — amend the book by ADR instead."""

TESTS = {
    # 16-the-core.md: From escapement + crown + linked liveness
    "COR-2": None,
    # 14-formats-protocols.md: Boot tape: tape to crown replay to serialize yields the same
    "FMT-3": None,
}
