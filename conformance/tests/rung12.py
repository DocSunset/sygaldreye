"""Rung 12 — stubs. Each entry: criterion id -> callable or None (pending).
Write the test when you reach this rung; the assertion is quoted from the book.
Never weaken a test to pass it — amend the book by ADR instead."""

TESTS = {
    # 17-conformance-evolution.md: Every acceptance criterion in this book exists
    "CNF-1": None,
    # 17-conformance-evolution.md: The harness runs entirely over the wire
    "CNF-2": None,
    # 17-conformance-evolution.md: The movement profile passes on a crownless build;
    "CNF-3": None,
    # 17-conformance-evolution.md: Introduce kind-v2 with a migration:
    "CNF-4": None,
    # 17-conformance-evolution.md: A core succession (N derives N+1) is admitted only
    "CNF-5": None,
    # 16-the-core.md: Movement-level and peer-level conformance run
    "COR-4": None,
    # 17-conformance-evolution.md: a chain of successions (origin, fix, additive, breaking, fix)
    "CNF-6.1": None,
    # 17-conformance-evolution.md: an "additive" succession that fails a predecessor criterion is
    "CNF-6.2": None,
    # 17-conformance-evolution.md: `osc@1.2.0` as a ref-sugar address resolves to the same hash as
    "CNF-6.3": None,
}
